<?xml version="1.0"?>
<config>
    <modules>
        <<%= moduleId %>>
            <version>1.0.0</version>
        </<%= moduleId %>>
    </modules>
    <global>
        <%_ if (includeBlock) { _%>
        <blocks>
            <<%= moduleId %>>
                <class><%= moduleName %>_Block</class>
            </<%= moduleId %>>
        </blocks>
        <%_ } _%>
        <%_ if (includeHelper) { _%>
        <helpers>
            <<%= moduleId %>>
                <class><%= moduleName %>_Helper</class>
            </<%= moduleId %>>
        </helpers>
        <%_ } _%>
    </global>
    <frontend>
        <%_ if (includeController) { _%>
        <routers>
            <<%= moduleId %>>
                <use>standard</use>
                <args>
                    <module><%= moduleName %></module>
                    <frontName><%= moduleFrontName %></frontName>
                </args>
            </<%= moduleId %>>
        </routers>
        <%_ } _%>
    </frontend>
</config>
